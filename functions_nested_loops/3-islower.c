#include " main.h"
/**
* main :Write a function that checks for lowercase character. 
* return : always 0
*/


int _islower(int c);
 {
	if(c<='z' && c>= 'a')
   {		
       return (1);
   }    
	return (0);	
 }
