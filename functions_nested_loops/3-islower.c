#include "main.h"
/**
* int _islower() : Write a function that checks for lowercase character 
* return: always 0
* char: c
*/


int _islower(int c);
 {

	if(c<='z' && c>= 'a')
   {
       return (1);
   }
	return (0);
 }
