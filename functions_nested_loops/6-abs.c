#include "main.h"
/**
 *_abs:computes the absolute value of an integer
 *Retrun Always 0
 */
int _abs(int c)
 {
	int a; 
       if (c < 0)
		{

		a = (c * -1);

		}
       	 
	       return(a); 
 }       
